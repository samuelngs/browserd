# Copy to /etc/browserd/browserd.env and set a strong token before starting.
BROWSERD_MCP_HTTP_TOKEN=change-me

# Optional loopback bind settings.
BROWSERD_MCP_HTTP_HOST=127.0.0.1
BROWSERD_MCP_HTTP_PORT=9223
